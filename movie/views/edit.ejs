<!DOCTYPE html>
<html lang="en">

<head>
    <title>Edit Car Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .form-container {
            background: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .btn-custom {
            background-color: #ff9800;
            color: white;
            transition: all 0.2s ease-in;
        }
        .btn-custom:hover {
            background-color: #e68900;
        }
    </style>
</head>

<body>
    <div class="container col-md-5 my-5">
        <div class="form-container">
            <h2 class="text-center mb-4">Edit Car Details</h2>
            <form method="post" action="/car-inventory" enctype="multipart/form-data">
                <input type="hidden" name="id" value="<%= record.id %>">
                
                <div class="mb-3">
                    <label class="form-label">Car Brand:</label>
                    <input type="text" class="form-control" name="brand" value="<%= record.brand %>" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Car Model:</label>
                    <input type="text" class="form-control" name="model" value="<%= record.model %>" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Car Price (₹):</label>
                    <input type="number" class="form-control" name="price" value="<%= record.price %>" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Car Color:</label><br>
                    <div class="ms-4">
                        <input type="checkbox" name="color" value="Red" <% if(record.color.includes("Red")) { %> checked <% } %> >
                        <label class="form-check-label">Red</label>
                    </div>
                    <div class="ms-4">
                        <input type="checkbox" name="color" value="Black" <% if(record.color.includes("Black")) { %> checked <% } %> >
                        <label class="form-check-label">Black</label>
                    </div>
                    <div class="ms-4">
                        <input type="checkbox" name="color" value="White" <% if(record.color.includes("White")) { %> checked <% } %> >
                        <label class="form-check-label">White</label>
                    </div>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Car Image:</label>
                    <input type="file" class="form-control" name="image">
                    <div class="mt-2">
                        <img src="<%= record.image %>" class="img-thumbnail" width="150">
                    </div>
                </div>
                
                <button type="submit" class="btn btn-custom w-100">Update Car</button>
            </form>
            <div class="text-center mt-3">
                <a href="/fetch" class="btn btn-outline-primary">Show Car Listings</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>